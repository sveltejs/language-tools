Step one, transform to valid typescript.tsx file

load and mutate with html parser 
* remove context=module script tag (add a placeholder or comment so we know where it stopped)
* remove script
* remove style tag and content
* wrap html body in <> </>
* change {#each items as item,i (foo)}..{/each} into {items.map( (foo,i) => <> ... </>)}
* change {#if (expr)} {:else} {/if} to 
{(() => { if (items.length = 5) { <>
        if content
    </>
} else {
    <>
        else content
    </>
}})}
* handle #await similarly
* change on:click -> onClick (remove : and toUpper first letter)

pass into typescript with custom transforms that do the following

* walk all exports that aren't inside the context=module, remove the export keyword and
* at bottom of file add
export default class {
    props: {
        name: string 
    }
}

with prop for each export.

ask typescript to return/emit the .d.ts file to go with the .svelte file it just processed.
