{// A leading line comment formatted next to the opening brace
value ? 'yes' : 'no'}

{
    // A leading line comment on the following line
    value ? 'yes' : 'no'}

{/* A leading block comment */ value ? 'yes' : 'no'}

{/
