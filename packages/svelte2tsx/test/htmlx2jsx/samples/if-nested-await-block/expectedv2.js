if(hello){
        { const y = await (hello.foo); 
        y;
    }
        { const y = await (x); 
        y;
    }
       { 
      hello;
    await (aPromise); }
    if(hi && bye){
             { try { const y = await (x); 
            y;
        } catch($$_e) { 
            
        }}
    } else if (cool){
              { 
            
        try { const y = await (x); 
            y;
        } catch($$_e) { 
            
        }}
    }else{
            { const y = await (x); 
            y;
        }
    }
}