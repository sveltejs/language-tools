if(name == "world"){
    const hello = name;
     { svelteHTML.createElement("h1", {}); hello; }
} else if (true){
    const hello = name;
     { svelteHTML.createElement("h1", {}); hello; }
}else{
    const hello = name;
     { svelteHTML.createElement("h1", {}); hello; }
}

if(typeof a === 'string'){
    const aStr = a;
    const aStr2 = aStr;

    a;
} else if (typeof a === 'number'){
    const aNum = a;
}

if(typeof a === 'string'){
    const aStr = a;
}

if(typeof a === 'string'){
    const aStr = a;
}else{
}